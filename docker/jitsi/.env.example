JITSI_DOMAIN=meet.example.com
JITSI_APP_ID=health-ease
JITSI_APP_SECRET=replace-with-a-long-random-secret
LETSENCRYPT_EMAIL=you@example.com

HTTP_PORT=80
HTTPS_PORT=443
CONFIG=./config
TZ=UTC

# Set this to the public IP of the Docker host if JVB cannot autodetect it.
JVB_ADVERTISE_IPS=